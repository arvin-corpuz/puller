apiVersion: v1
kind: Service
metadata:
  name: puller
  namespace: default
  labels:
    app: puller
spec:
  selector:
    app: puller
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
